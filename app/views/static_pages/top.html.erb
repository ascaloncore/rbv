<div class="space-y-6">
  <h1 class="text-4xl md:text-5xl font-bold text-center mb-8 text-[#FFFFFF]">ダッシュボード</h1>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <!-- 在庫金額総額（負債） -->
    <div class="bg-[#1A1C23] rounded-2xl border border-[#2E323A] border-l-4 border-l-red-500 p-6 shadow-lg hover:border-[#3E434A] transition-colors">
      <h2 class="text-lg text-[#94A3B8] mb-2 font-semibold">在庫金額総額（リスク資産）</h2>
      <p class="text-4xl md:text-5xl font-bold text-red-500 mb-2">
        <%= number_to_currency(@total_inventory_value, unit: "¥", format: "%u %n", precision: 0) %>
      </p>
      <p class="text-sm text-[#64748B] mt-2">ステータスが「完了」以外の在庫の帳簿価額の合計</p>
    </div>

    <!-- 知識資産総額（資産） -->
    <div class="bg-[#1A1C23] rounded-2xl border border-[#2E323A] border-l-4 border-l-emerald-500 p-6 shadow-lg hover:border-[#3E434A] transition-colors bg-emerald-500/5">
      <h2 class="text-lg text-[#94A3B8] mb-2 font-semibold">獲得知識資産（資産）</h2>
      <p class="text-4xl md:text-5xl font-bold text-emerald-400 mb-2">
        <%= number_to_currency(@total_knowledge_asset_value, unit: "¥", format: "%u %n", precision: 0) %>
      </p>
      <p class="text-sm text-[#64748B] mt-2">ステータスが「完了」の在庫の帳簿価額の合計</p>
    </div>
  </div>

  <!-- 減損損失累計額 -->
  <div class="bg-[#1A1C23] rounded-2xl border border-[#2E323A] p-6 shadow-lg hover:border-[#3E434A] transition-colors">
    <h2 class="text-lg text-[#94A3B8] mb-2 font-semibold">減損損失累計額</h2>
    <p class="text-3xl md:text-4xl font-bold text-[#EF4444] mb-2">
      <%= number_to_currency(@total_impairment_loss, unit: "¥", format: "%u %n", precision: 0) %>
    </p>
    <p class="text-sm text-[#64748B] mt-2">ステータスが「完了」以外の在庫の減損損失の合計</p>
  </div>
</div>
